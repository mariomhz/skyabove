const labelBase = 'uppercase tracking-[0.3em] text-black/30 font-medium';

/** Label used in hero section and footer — larger sizing */
export const labelClass = `text-xs sm:text-sm md:text-base ${labelBase}`;

/** Label used in dashboard rows and disclaimer — compact sizing */
export const labelClassSm = `text-[11px] sm:text-xs md:text-sm ${labelBase}`;
